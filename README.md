# Smart-route-optimizer-using-greedy-and-dp
This project finds the shortest route between multiple locations. It uses a Greedy algorithm for a fast approximate solution and Dynamic Programming (DP) for the optimal route. Greedy selects the nearest next location, while DP (TSP) checks all paths with memoization. The system compares both and outputs the best route with minimum distance.
